def print_pascal_triangle(n):
    for i in range(n):
        for j in range(n-i-1):
            print(end=" ")
        for j in range(i+1):
            print(int((11**i) // (11**(i-j) * 11**j)), end=" ")
        print()

print_pascal_triangle(5)
